# Configuration file for QMIX RNN evaluation on Road environments
CHECKPOINT_PATH: "outputs/ToyExample-v2/qmix_rnn/2025-04-26_10-45-27.392377/"  # Path to the checkpoint directory containing config.yaml and the checkpoints folder
STEP: 150  # The step number of the checkpoint to evaluate
VMAPPED_SEED: 0  # Which vmapped seed to use (if multiple were used during training)

# Test parameters
TEST_NUM_ENVS: 1000  # Number of parallel environments to use during evaluation
TEST_NUM_STEPS: 50  # Number of steps to run in each environment during evaluation

# Randomness control
SEED: 42  # Random seed for the evaluation

# hydra params
hydra:
  run:
    dir: ${CHECKPOINT_PATH}/eval/${now:%Y-%m-%d_%H-%M-%S.%f}  # Directory to save evaluation results