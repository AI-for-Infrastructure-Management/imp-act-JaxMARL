map: ToyExample-v2  # Options: ToyExample-v2, Cologne-v1, CologneBonnDusseldorf-v1
policy: segment_based_heuristic  # Options: do_nothing, random, segment_based_heuristic
num_envs: 10000
num_steps: 50
norm_constant: 1e6
plot_histogram: False
double_precision_mode: False
policy_parameters:
  inspection_interval: 5  # Every n timesteps the policy will perform inspection (action 1)
  repair_threshold: 2     # When observation exceeds this value, repair action is taken (action 2)
  prioritization_enabled: True
  prioritization_key: "volumes" # Options: "segment_lengths", "costs", "volumes"
  prioritization_sign: "positive"
  top_k: 7

# hydra params
hydra:
  run:
    dir: outputs/${map}/segment_based_heuristic/eval/${now:%Y-%m-%d_%H-%M-%S.%f}